# 构建产物
bin/
obj/

# 发布成品（用 README 里的 dotnet publish 自行生成）
dist/

# 运行时生成（位于 exe / 程序同目录）
work/
config.json

# 编辑器 / 系统
.vs/
.idea/
*.user
Thumbs.db
desktop.ini

# 大体积样本 APK（不要提交，GitHub 单文件上限 100MB）
*.apk
